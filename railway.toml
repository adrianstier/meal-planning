[build]
builder = "nixpacks"
buildCommand = "chmod +x railway-build.sh && ./railway-build.sh"

[deploy]
startCommand = "chmod +x run-migrations.sh && ./run-migrations.sh && gunicorn app:app"
restartPolicyType = "on_failure"
